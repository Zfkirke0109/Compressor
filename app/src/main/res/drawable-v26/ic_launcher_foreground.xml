<?xml version="1.0" encoding="utf-8"?>
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <!--
      The launcher glyph (galaxy + compress mark) is baked into
      @mipmap/ic_launcher_bg, centred and mask-safe. This foreground layer is
      intentionally transparent so the glyph is not drawn twice; the themed /
      monochrome icon is supplied separately by @drawable/ic_launcher_monochrome.
    -->
    <path
        android:fillColor="#00000000"
        android:pathData="M0,0h108v108h-108z" />
</vector>
