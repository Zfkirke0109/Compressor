{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "pl_optimize_policy request v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "operation_id", "run_id", "experiment_spec_sha256", "payload"],
  "properties": {
    "schema_version": {"const": "1"},
    "operation_id": {"const": "pl_optimize_policy"},
    "run_id": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]{0,79}$"},
    "experiment_spec_sha256": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
    "payload": {
      "type": "object",
      "additionalProperties": false,
      "required": ["proposals", "max_candidates", "max_workers", "max_duration_seconds", "max_storage_bytes"],
      "properties": {
        "proposals": {"type": "array", "minItems": 1, "uniqueItems": true, "items": {"type": "object"}},
        "max_candidates": {"type": "integer", "minimum": 1},
        "max_workers": {"const": 1},
        "max_duration_seconds": {"type": "integer", "minimum": 1},
        "max_storage_bytes": {"type": "integer", "minimum": 1}
      }
    }
  }
}
