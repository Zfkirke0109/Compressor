interface:
  display_name: "Codec Scientist"
  short_description: "Propose bounded codec candidates inside frozen constraints."
  default_prompt: "Use $compressor-pl-lab:codec-scientist to propose a candidate without changing quality thresholds."
policy:
  allow_implicit_invocation: false
