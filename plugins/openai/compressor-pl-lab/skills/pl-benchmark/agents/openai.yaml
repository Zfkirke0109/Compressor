interface:
  display_name: "PL Benchmark"
  short_description: "Run a bounded baseline-versus-candidate benchmark."
  default_prompt: "Use $compressor-pl-lab:pl-benchmark to compare a declared candidate with its frozen baseline."
policy:
  allow_implicit_invocation: false
